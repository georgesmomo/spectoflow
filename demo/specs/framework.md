## Purpose
spectoflow: an agent-agnostic SDD framework with a real-time control plane.

## ADDED Requirements
### Requirement: Markdown artifacts
#### Scenario: human-readable plans
- **WHEN** a plan is written
- **THEN** it is markdown with checkbox tasks the dashboard can parse
